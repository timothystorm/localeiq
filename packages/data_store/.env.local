# LocaleIQ Data Store Environment Configuration
#
# This is a template file. Copy it to .env and fill in your actual values.
# Usage: cp .env.local .env
#
# IMPORTANT: .env is gitignored and should NEVER be committed to version control

# PostgreSQL Database Configuration
DB_USER=localeiq_user
DB_PASSWORD=localeiq_pass
DB_NAME=localeiq
DB_HOST=localhost
DB_PORT=5432

# Database connection URL (used by SQLAlchemy)
# This variable reference syntax works in docker-compose but not in Python
# The application will construct this from individual values above
DB_URL="postgresql+psycopg2://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# pgAdmin Web Interface Configuration
PGADMIN_EMAIL=admin@localeiq.com
PGADMIN_PASSWORD=admin123
